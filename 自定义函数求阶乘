#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int fanc(int x)
{
    int a=1,i;
    for(i=1;i<=x;i++)
    {
        a=a*i;
    }
    return a;
}

int main()
{
    int n;
    scanf("%d",&n);
    printf("%ld",fanc(n));

}
