#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int a[20],i,j,n=0;
    for(i=0;i<20;i++)
    {
        scanf("%d",&a[i]);
        for(j=0;j<i;j++)
        {
            if(a[j]==a[i]) break;
        }
        if(j==i) n++;
    }
    printf("%d\n",n);
    return 0;
}
