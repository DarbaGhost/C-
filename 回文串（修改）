#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main()
{
    int i,n;
    char a[80];
    gets(a);    
    n=strlen(a);
    for(i=0;i<n/2;i++)
    {
        if(a[i]!=a[n-i-1])
        {printf("N");break;}
    }
    if(i==n/2) printf("Y");

    return 0;
}
