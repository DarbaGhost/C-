#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char s[100]="";
    char a[30];
    for(int i=1;i<=3;i++)
    {
        gets(a);
        strcat(s,a);
    }
    printf("%s", s);
}
