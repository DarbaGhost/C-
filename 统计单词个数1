#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int i,n,sum=0;
    char a[100];
    gets(a);
    n=strlen(a);
    for(i=0;i<=n-1;i++)
    {
        if(a[i]!=' '&&(a[i+1]==' '||a[i+1]=='\0'))
            sum=sum+1;
    }
    printf("%d",sum);

    return 0;
}
